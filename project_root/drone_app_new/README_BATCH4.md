# Batch-4 scaffold (AI Director) — README

## Purpose
This scaffold provides safe placeholders for:
- Flutter AI UI (ai_panel)
- Server AI endpoints (FastAPI)
- Laptop AI director (vision + GPT stubs)
- Radxa safe mission intake (no motor outputs)
- Memory store (server-side JSON)

## How to run (quick)
1. Install server deps:
